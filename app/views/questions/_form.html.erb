<%= form_for @question do |f| %>
  <%= f.text_field :title, placeholder: "Question Title" %>
  <%= f.text_area :body, placeholder: "Question Body" %>
  <%= f.button :submit, class: "btn btn-large btn-info" %>
<% end %>
