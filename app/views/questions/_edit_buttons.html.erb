<% if signed_in? %>
  <% if current_user.id == @question.user_id %>
    <%= link_to "Edit", edit_question_path(@question), class: "btn btn-large btn-warning" %>
    <%= link_to "Delete", @question, method: :delete, class: "btn btn-large btn-danger"%>
  <% end %>
<% end %>
